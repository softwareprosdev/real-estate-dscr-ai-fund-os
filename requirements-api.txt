# Core API dependencies — used in the Docker image.
# Heavy ML training deps (torch, stable-baselines3, gymnasium) are in pyproject.toml[dev]
# and are only needed for RL training, not for running the API server.
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
numpy==1.26.4
scipy==1.13.0
scikit-learn==1.4.2
xgboost==2.0.3
httpx==0.27.0
python-dotenv==1.0.1
structlog==24.1.0
